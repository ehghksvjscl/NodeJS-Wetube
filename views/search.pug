extends layouts/main
include mixins/videoBlock

block content
    .search__header
        h3 검색 : #{searchingBy}
    .search__videos
        each item in video
        +videoBlock({
            title:item.title,
            views:item.views,
            videoFile:item.videoFile
        })